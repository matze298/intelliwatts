services:
  sandbox:
    build:
      context: ..
      dockerfile: sandbox/Dockerfile
    runtime: runsc
    volumes:
      - ..:/app
    working_dir: /app
    environment:
      - ENV=development
      - PYTHONPATH=/app
      - UV_PROJECT_ENVIRONMENT=/opt/venv
    network_mode: bridge
