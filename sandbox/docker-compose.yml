services:
  sandbox:
    build:
      context: ..
      dockerfile: sandbox/Dockerfile
    runtime: runsc
    volumes:
      - ..:/app
    environment:
      - ENV=development
    network_mode: bridge
